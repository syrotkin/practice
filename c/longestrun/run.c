#include <stdio.h>
#include <stdlib.h>
#include <string.h>




int longest_run(char* s) {
  // need to find which character it is and how many they are
  // need a hash table
  // can assume it's ASCII and use a 256-long array, indexed by the character
}

int main() {
  
  char* s1 = "aabbbccdddee";
  

  return 0;
}
